# use `pip freeze > requirements.txt` to generate file with all packages in your environment
# use `pip install -r requirements.txt` to install all required packages
numpy>=1.16.0
flask
face-recognition
scikit-learn
gunicorn
tqdm
