from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Find a path through similar images to end up with an ugly celebrity you indirectly look like',
    author='Robert van Straalen',
    license='',
)
